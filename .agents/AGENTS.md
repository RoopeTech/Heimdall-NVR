# Project Guidelines

## Changelogs and Versioning
- ALWAYS post change notes to `CHANGELOG.md` when pushing fixes or features to Git.
- ALWAYS bump the version number in the `VERSION` file accordingly so that the in-app update mechanism triggers for the user.

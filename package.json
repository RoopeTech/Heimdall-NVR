{
  "name": "Heimdall-nvr",
  "version": "0.1.0",
  "description": "Cross-Platform IP Camera Viewer & NVR with Local & Web UIs",
  "scripts": {
    "frontend:install": "cd frontend && npm install",
    "frontend:build": "cd frontend && npm run build",
    "backend:install": "pip install -r backend/requirements.txt",
    "backend:start": "python backend/main.py",
    "setup": "npm run frontend:install && npm run frontend:build && npm run backend:install",
    "start": "npm run backend:start"
  }
}
